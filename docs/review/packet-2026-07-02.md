# Review Packet — 2026-07-02

**Context**: `terminal-agent-lab` — scoped-down TMAX replication (arXiv:2606.23321). Current phase per `docs/PROJECT_SPEC.md`: **Phase 1 (eval harness)**. This packet covers the Phase 1 milestone: paper distillation → harness implementation → first real-model bug found and fixed via the new transcript logging.

## 1. What changed

```
0f01207 fix(harness): reject mixed command+submit replies; log full rollout transcripts
2d0ebbf feat(harness): Phase 1 eval harness — agent loop, sandbox, verifier, pass@k runner
4e2f6be docs(notes): distill TMAX paper into concept notes + scope decision
2570f2c chore: seed project scaffold   <- base
```

`git diff --stat 2570f2c..HEAD`: **40 files, +2,126 lines** — 5 paper notes + 2 decision notes (`docs/notes/`), 12 harness modules (`harness/`), 7 test files (36 tests, all green; 8 need Docker), 3 toy tasks (`tasks/toy-{hello,pipeline,service}/`), uv project setup.

Architecture (one line per file): `config.py` YAML+CLI config · `llm.py` Anthropic/OpenRouter over httpx · `prompts.py` system prompt + observation templates · `command_parser.py` reply → single bash command · `sandbox.py` Docker lifecycle (resource-limited, no host mounts) · `shell.py` persistent shell over one `docker exec`, 120 s/cmd timeout · `episode.py` the agent loop · `verifier.py` reward = pytest pass fraction, tests copied in at verify time · `runner.py` k rollouts → unbiased pass@k → JSONL + per-rollout transcript sidecars · `cli.py` `uv run harness --task tasks/<id> --model <m> --k 4`.

## 2. Key code

### harness/command_parser.py (full — carries decision 002 rev 2)

```python
"""Extract the single bash command from a model reply.

Protocol (mini-SWE-agent style, see decision 002): the model must reply with
exactly one fenced code block containing one bash command; it submits by
replying with a block containing ONLY the SUBMIT_COMMAND. Mixing commands and
the submit marker in one reply is a format error — the loop feeds the error
back so the model can run the commands and submit separately.
"""

from __future__ import annotations

import re
from dataclasses import dataclass

SUBMIT_COMMAND = "echo COMPLETE_TASK_AND_SUBMIT"

_CODE_BLOCK_RE = re.compile(r"```(?:bash|sh|shell)?[ \t]*\n(.*?)```", re.DOTALL)

NO_BLOCK = "no_code_block"
MULTIPLE_BLOCKS = "multiple_code_blocks"
MIXED_SUBMIT = "mixed_command_and_submit"


@dataclass
class ParsedReply:
    command: str | None
    submitted: bool = False
    error: str | None = None


def _is_submit_only(block: str) -> bool:
    return block.strip() == SUBMIT_COMMAND


def _contains_submit_line(block: str) -> bool:
    return any(line.strip() == SUBMIT_COMMAND for line in block.splitlines())


def parse_reply(text: str) -> ParsedReply:
    blocks = [b.strip() for b in _CODE_BLOCK_RE.findall(text)]
    blocks = [b for b in blocks if b]
    if not blocks:
        return ParsedReply(None, error=NO_BLOCK)
    if len(blocks) == 1:
        block = blocks[0]
        if _is_submit_only(block):
            return ParsedReply(block, submitted=True)
        if _contains_submit_line(block):
            # Commands and the submit marker in one block: refuse rather than
            # guess which half the model meant (this silently ate rollouts
            # before decision 002 rev 2).
            return ParsedReply(None, error=MIXED_SUBMIT)
        return ParsedReply(block)
    if any(_contains_submit_line(b) for b in blocks):
        return ParsedReply(None, error=MIXED_SUBMIT)
    return ParsedReply(None, error=MULTIPLE_BLOCKS)
```

### harness/episode.py — the loop (full `run_episode`; dataclasses elided)

```python
def run_episode(llm, shell, instructions: str, cfg: RunConfig) -> EpisodeResult:
    result = EpisodeResult()
    messages: list[dict] = [{"role": "user", "content": format_task(instructions)}]

    for _ in range(cfg.max_steps):
        reply = llm.chat(SYSTEM_PROMPT, messages)
        result.input_tokens += reply.input_tokens
        result.output_tokens += reply.output_tokens
        messages.append({"role": "assistant", "content": reply.text})

        parsed = parse_reply(reply.text)
        if parsed.error:
            observation = format_error(parsed.error)
            result.steps.append(Step(reply.text, None, observation, format_error=parsed.error))
        elif parsed.submitted:
            result.steps.append(Step(reply.text, parsed.command, "(submitted)"))
            result.submitted = True
            result.stop_reason = "submitted"
            return result
        else:
            shell_result = shell.run(parsed.command)
            output, truncated = _truncate(shell_result.output, cfg.max_output_chars)
            observation = format_observation(
                output, shell_result.exit_code, truncated, shell_result.timed_out
            )
            result.steps.append(
                Step(reply.text, parsed.command, observation,
                     exit_code=shell_result.exit_code, timed_out=shell_result.timed_out)
            )
        messages.append({"role": "user", "content": observation})

        if result.input_tokens + result.output_tokens > cfg.token_budget:
            result.stop_reason = "token_budget"
            return result

    result.stop_reason = "max_steps"
    return result
```

Format errors count against `max_steps` (a model stuck on formatting can't loop forever). The third decision-heavy file is `harness/shell.py`: persistent shell over one long-lived `docker exec -i ... bash`, sentinel + exit code per command; on the 120 s timeout the shell is killed and restarted — container/background processes survive, shell-local state (cwd, env) is lost, and the observation says so explicitly.

## 3. Results

All runs: Haiku = `claude-haiku-4-5-20251001`, Sonnet = `claude-sonnet-4-6`, temperature 1.0, k = n = 4, `evals/results/*.jsonl` (+ `transcripts/` since 0f01207).

| Task | Model | pass@1 | successes | steps/rollout |
|---|---|---|---|---|
| toy-hello | Haiku | 1.0 | 4/4 | 3,3,3,5 |
| toy-hello | Sonnet | 1.0 | 4/4 | 2,2,2,2 |
| toy-pipeline (pre-fix) | Haiku | **0.25** | 1/4 | 3,1,3,1 |
| toy-pipeline (post-fix) | Haiku | **1.0** | 4/4 | 4,8,6,4 |
| toy-service | Haiku | 1.0 | 4/4 | 9,6,7,10 |

- The pre-fix 0.25 was a **harness artifact, not task difficulty**: the parser accepted a reply mixing solution commands with the submit marker as an instant submit (step-1 submit, work discarded). Post-fix, the same reply shape triggers corrective feedback; rollout 1 recovered live from exactly this at step 0 and solved in 8 steps.
- Paper comparison: no direct analogue (toys are sanity checks, meant to saturate). Nearest anchors: paper Table 12 has Haiku 4.5 at 60.8 ± 0.6 TB-Lite under their harness, i.e. real benchmark tasks are far from saturated; paper Table 1 reports mean 16.3 turns/task on TMAX-15K vs. our 2–10 on toys — consistent with toys sitting well below "short" bucket difficulty. Real comparison starts in Phase 2 (Gemini-Flash-class pass@1 42% / pass@4 50% on TMAX-15K is the row we'll line up against).
- Phase 1 exit criteria: 3/3 toy tasks pass end-to-end; second API model run on toy-hello only so far (see Q4).

## 4. Open questions

1. **Mixed-submit policy**: we now *reject* replies that mix commands with the submit marker and feed back "run commands and submit in separate messages" (decision 002 rev 2). Alternative: leniently execute the commands and ignore the marker. Rejection is unambiguous but burns a step and arguably penalizes models for a prompt we control; leniency risks executing half-intended scripts. Which would you ship, and does rejection bias cross-model comparisons (models differ in how often they mix)?
2. **Timeout vs. persistence**: on a 120 s timeout we kill and restart the shell (losing cwd/env, telling the model). Alternative: wrap every command in `timeout 120 bash -c ...`, which preserves the session but changes command semantics (subshell, no bg-process handoff). Is our contract fair, or a hidden difficulty knob the paper's harness doesn't have?
3. **pass@k protocol for Phase 2**: paper never states its estimator; we use Chen et al. 2021 unbiased with n = k = 4 by default and log raw rewards. For the Phase 2 report (30-task subsample, 2 models), is n = 8 rollouts (matching Table 1's pass@1/4/8) worth ~2× the API cost over n = 4?
4. **Milestone gate**: exit criteria say two API models on the 3 toys; Sonnet has only run toy-hello. Run Sonnet on the other two (~$0.10) and call Phase 1 done, or is there anything above you'd want hardened first before I start the Phase 2 sampler (`pipeline/axes.yaml`)?

## 5. Journal delta (latest entry)

> **2026-07-02 — First real-model bug: mixed submit replies + transcript logging**
>
> **Built**: investigated toy-pipeline rollouts 1 & 3 (Haiku, step-1 submit, reward 0). Root cause: the rev-1 parser matched the submit marker against only the *first line* of the code block, so a reply mixing the marker with solution commands was accepted as an instant submit and the work silently discarded. Fixed per [[002-harness-conventions]] rev 2: submit is only valid as the sole content of the reply's single block; any command+marker mix is a new `mixed_command_and_submit` format error fed back as "run commands and submit in separate messages". Also added the missing observability: full per-step transcripts now land in `evals/results/transcripts/*.json`, linked from each JSONL row — the original bug was undiagnosable from summary rows alone.
>
> **Evidence**: re-ran toy-pipeline k=4 with Haiku — pass@1 went 0.25 → 1.0 (4/4). Rollout 1's first reply hit the new error live: Haiku hallucinated an entire multi-turn session in one message (commands, fake outputs, submit), got the corrective feedback, and recovered to solve in 8 steps.
>
> **Interview takeaway**: small models don't just fail tasks, they fail the *protocol* — the harness's format-error feedback loop is doing real work (here worth 0.75 pass@1 on a trivial task). This is exactly why the paper found simple harnesses beat Terminus-2 for small models (§C, Table 12): every bit of protocol strictness must come with a recovery path, not a silent misparse.

Further reading: `docs/notes/paper/harness.md` (paper contract), `docs/notes/decisions/001-scope.md` (deviations), `docs/notes/decisions/002-harness-conventions.md` (protocol details).
