The file `/data/sales.csv` has columns `region,amount`.

Write `/output/summary.csv` with:
- header line `region,total`
- one row per region with the summed amount formatted to two decimal places
- rows sorted alphabetically by region

Example row: `north,15.00`
